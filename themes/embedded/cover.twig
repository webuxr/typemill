{% extends '/partials/layoutCover.twig' %}

{% block title %}{{ settings.title }}{% endblock %}

{% block content %}

	<div class="lead">

		{{ content }}

		<div class="actionLink">
			<a href="{{ navigation[0].urlRel }}">{{ settings.themes.embedded.start ? settings.themes.embedded.start : 'Start'}}</a>

			{% if settings.setup  %}
				<a href="{{ base_url }}/setup">Setup</a>
			{% endif %}
		</div>
	</div>

{% endblock %}